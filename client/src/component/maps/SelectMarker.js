import React from "react";

const TruckMarker = ({ text }) => {
  return (
    <div
      style={{
        height: "10px",
        width: "10px",
        backgroundColor: "green",
        borderRadius: "100%"
      }}
    ></div>
  );
};

export default TruckMarker;
