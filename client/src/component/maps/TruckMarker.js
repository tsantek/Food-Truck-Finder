import React from "react";

const TruckMarker = ({ text }) => {
  return (
    <div
      style={{
        height: "15px",
        width: "15px",
        backgroundColor: "red",
        borderRadius: "100%"
      }}
    ></div>
  );
};

export default TruckMarker;
