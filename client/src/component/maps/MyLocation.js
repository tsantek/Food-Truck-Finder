import React from "react";

const MyLocation = ({ text }) => {
  return (
    <div
      style={{
        height: "15px",
        width: "15px",
        backgroundColor: "blue",
        borderRadius: "100%"
      }}
    ></div>
  );
};

export default MyLocation;
