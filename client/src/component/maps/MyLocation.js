import React from "react";

const MyLocation = ({ text }) => {
  return (
    <div
      style={{
        height: "10px",
        width: "10px",
        backgroundColor: "blue",
        borderRadius: "100%"
      }}
    ></div>
  );
};

export default MyLocation;
